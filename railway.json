{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan migrate:fresh --seed --force && php -S 0.0.0.0:$PORT"
  }
}