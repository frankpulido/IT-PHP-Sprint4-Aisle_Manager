{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm run build && chmod -R 755 public/styles"
  },
  "deploy": {
    "startCommand": "php artisan migrate:fresh --seed --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "cronSchedule": "0 0 * * *"
  }
}